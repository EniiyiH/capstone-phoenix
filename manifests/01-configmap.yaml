apiVersion: v1
kind: ConfigMap
metadata:
  name: taskapp-config
  namespace: taskapp
data:
  POSTGRES_DB: "taskapp"
  POSTGRES_HOST: "postgres"
  POSTGRES_PORT: "5432"
  DATABASE_HOST: "postgres"
  DATABASE_NAME: "taskapp"
  DATABASE_PORT: "5432"  
  FLASK_ENV: "production"
  CORS_ORIGINS: "https://taskapp.eniiyi.name.ng"
