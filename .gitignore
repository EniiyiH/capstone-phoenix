# --- Secrets & state: NEVER commit. The grader runs `git log -p` looking for these. ---
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
*.tfvars
!*.tfvars.example
crash.log

# kubeconfig & cluster joins
kubeconfig
*kubeconfig*
k3s.yaml
node-token
*.token

# secrets / env
.env
.env.*
!.env.example
*secret*.yaml
!*secret*.example.yaml
*.pem
id_*
*.key
id_rsa*

# argo / helm local
charts/*.tgz

#ansible
*.retry
inventory.ini
kubeconfig*

# os / editor
.DS_Store
*.swp
.idea/
.vscode/
